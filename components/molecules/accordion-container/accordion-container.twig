{#
/**
 * @file
 * Vartheme BS5 Horizonaid Starter Kit implementation for the Accordion Container component.
 *
 * Available config:
 * - id: Accordion container ID used to group nested items so only one stays
 *       open at a time. Leave empty to auto-generate.
 * - always_open: (true|false) Allow multiple items to stay open at once.
 *                Defaults to `false`.
 */
#}

{# AI guidance:
 # - Do not alter property descriptions or existing logic unless new
 #   properties are added.
 # - Keep the structure: 1) variables, 2) logical variable manipulations,
 #   3) print the HTML.
 # - Always handle classes as arrays with `merge` (never string
 #   concatenation) to avoid duplicate classes and data type conflicts.
 #}

{# Variables: boolean props.
 # SDC validates `type: boolean` props and applies their schema defaults,
 # so the values arrive as real booleans — only a presence fallback is
 # needed here, never type coercion.
 #}
{% set always_open = always_open ?? false %}

{# Logic: resolve ID. #}
{% set accordion_id = id|default('')|trim %}
{% if accordion_id is empty %}
  {% set accordion_id = 'accordion-' ~ random(1000000) %}
{% endif %}

{# These variables are passed down to child accordion-block components via Twig context. #}
{% set accordion_parent_id   = accordion_id %}
{% set accordion_always_open = always_open %}

{# Logic: resolve state flags. #}
{% set has_slot_content = accordion_content is defined and accordion_content|render|trim is not empty %}

{# Logic: build attributes. #}
{% set attributes = attributes.setAttribute('id', accordion_id).addClass(['accordion']) %}

{# Print the HTML. #}
<div{{ attributes }}>
  {% if has_slot_content %}
    {{ accordion_content }}
  {% else %}
    {% set demo_1_content %}<p class="mb-0">Lorem ipsum dolor sit amet, consectetur adipisicing elit.</p>{% endset %}
    {{ include('vartheme_bs5_horizonaid:accordion-block', {
      title: 'Accordion 1',
      heading_level: 2,
      open_by_default: true,
      parent_id: accordion_id,
      always_open: always_open,
      accordion_content: demo_1_content,
    }, with_context: false) }}

    {% set demo_2_content %}<p class="mb-0">Sed ut perspiciatis unde omnis iste natus error sit voluptatem.</p>{% endset %}
    {{ include('vartheme_bs5_horizonaid:accordion-block', {
      title: 'Accordion 2',
      heading_level: 2,
      open_by_default: false,
      parent_id: accordion_id,
      always_open: always_open,
      accordion_content: demo_2_content,
    }, with_context: false) }}
  {% endif %}
</div>
