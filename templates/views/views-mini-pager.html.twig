{#
/**
 * @file
 * Vartheme BS5 Horizon Aid's implementation for a views mini-pager.
 *
 * Available variables:
 * - heading_id: Pagination heading ID.
 * - items: List of pager items.
 *
 * @see template_preprocess_views_mini_pager()
 *
 * @ingroup themeable
 */
#}
{% include 'vartheme_bs5_horizonaid:pagination' with {
  items: items,
  alignment: 'center',
  pagination_utility_classes: ['mt-3']
} %}
